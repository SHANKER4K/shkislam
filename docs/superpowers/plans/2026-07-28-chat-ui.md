# Chat UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a chat page where the user can type a question and get a response, using `ai-elements` components.

**Architecture:** Client component at `/chat` uses `Conversation` + `Message` + `PromptInput` from ai-elements. Plain `useState` + `fetch` to POST `/api/chat`. The API route is a stub that returns a hardcoded Arabic response — the user will swap in their real backend later.

**Tech Stack:** Next.js 16 App Router, ai-elements (shadcn registry), Tailwind CSS v4, lucide-react.

## Global Constraints

- All imports use `@/` alias (maps to project root via tsconfig `baseUrl: "."`)
- ai-elements components live at `src/components/ai-elements/` → import from `@/src/components/ai-elements/...`
- Site is RTL (`dir="rtl"`, `lang="ar"`) — test with Arabic text
- No new npm dependencies beyond `ai-elements` components (already installed)
- No AI SDK `useChat` — plain `fetch` instead

---

### Task 1: Add TooltipProvider to root layout

**Files:**
- Modify: `app/layout.tsx`

**Interfaces:**
- Consumes: nothing
- Produces: `TooltipProvider` wrapping children (required by ai-elements shadcn components)

- [ ] **Step 1: Add TooltipProvider import and wrap**

`app/layout.tsx`:
```tsx
// add import at top
import { TooltipProvider } from "@/components/ui/tooltip";

// wrap inside ThemeProvider, around children
<TooltipProvider>
  {children}
  <Toaster position="top-center" dir="rtl" />
</TooltipProvider>
```

- [ ] **Step 2: Run dev server to verify no errors**

```bash
cd /home/ismail/Documents/projects/JS/shkislam && bun run dev
```
Expected: server starts, no build errors.

- [ ] **Step 3: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: add TooltipProvider for ai-elements components"
```

---

### Task 2: Create API route stub

**Files:**
- Create: `app/api/chat/route.ts`

**Interfaces:**
- Consumes: nothing (stub)
- Produces: `POST /api/chat` — accepts `{ message: string }`, returns `{ response: string }`

- [ ] **Step 1: Create the route file**

```ts
// app/api/chat/route.ts

export async function POST(req: Request) {
  const { message } = await req.json();

  // ponytail: hardcoded stub — swap for real backend when ready
  const response = `وعليكم السلام ورحمة الله وبركاته. لقد استلمت سؤالك: "${message}". سيتم ربط النظام الحقيقي قريباً.`;

  return Response.json({ response });
}
```

- [ ] **Step 2: Verify route is reachable**

```bash
curl -X POST http://localhost:3000/api/chat \
  -H 'Content-Type: application/json' \
  -d '{"message":"ما هو التوحيد؟"}'
```
Expected: `{"response":"وعليكم السلام ورحمة الله وبركاته. لقد استلمت سؤالك: \"ما هو التوحيد؟\". سيتم ربط النظام الحقيقي قريباً."}`

- [ ] **Step 3: Commit**

```bash
git add app/api/chat/route.ts
git commit -m "feat: add chat API route stub"
```

---

### Task 3: Create the chat page

**Files:**
- Create: `app/chat/page.tsx`

**Interfaces:**
- Consumes: `POST /api/chat` endpoint
- Produces: `/chat` page with full chat UI

- [ ] **Step 1: Create the chat page**

```tsx
// app/chat/page.tsx
"use client";

import { useState } from "react";
import {
  Conversation,
  ConversationContent,
  ConversationEmptyState,
  ConversationScrollButton,
} from "@/src/components/ai-elements/conversation";
import {
  Message,
  MessageContent,
  MessageResponse,
} from "@/src/components/ai-elements/message";
import {
  PromptInput,
  type PromptInputMessage,
  PromptInputTextarea,
  PromptInputSubmit,
} from "@/src/components/ai-elements/prompt-input";
import { MessageSquare } from "lucide-react";

type ChatMessage = {
  role: "user" | "assistant";
  content: string;
};

export default function ChatPage() {
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [input, setInput] = useState("");
  const [status, setStatus] = useState<"idle" | "loading">("idle");

  const handleSubmit = async (message: PromptInputMessage) => {
    const text = message.text?.trim();
    if (!text) return;

    setMessages((prev) => [...prev, { role: "user", content: text }]);
    setInput("");
    setStatus("loading");

    try {
      const res = await fetch("/api/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ message: text }),
      });
      const data = await res.json();
      setMessages((prev) => [
        ...prev,
        { role: "assistant", content: data.response },
      ]);
    } catch {
      setMessages((prev) => [
        ...prev,
        { role: "assistant", content: "حدث خطأ في الاتصال. حاول مرة أخرى." },
      ]);
    } finally {
      setStatus("idle");
    }
  };

  return (
    <main className="flex-1 flex flex-col max-w-3xl mx-auto w-full p-4 h-[calc(100vh-3.5rem)]">
      <h1 className="font-arabic text-2xl font-bold mb-4 text-center">المحادثة</h1>
      <Conversation className="flex-1">
        <ConversationContent>
          {messages.length === 0 ? (
            <ConversationEmptyState
              icon={<MessageSquare className="size-12" />}
              title="ابدأ محادثة"
              description="اكتب سؤالك في الأسفل لبدء المحادثة"
            />
          ) : (
            messages.map((msg, i) => (
              <Message from={msg.role} key={i}>
                <MessageContent>
                  <MessageResponse>{msg.content}</MessageResponse>
                </MessageContent>
              </Message>
            ))
          )}
        </ConversationContent>
        <ConversationScrollButton />
      </Conversation>

      <PromptInput
        onSubmit={handleSubmit}
        className="mt-4 w-full relative"
      >
        <PromptInputTextarea
          value={input}
          placeholder="اكتب سؤالك هنا..."
          onChange={(e) => setInput(e.currentTarget.value)}
          className="pr-12"
        />
        <PromptInputSubmit
          status={status === "loading" ? "submitted" : "ready"}
          disabled={!input.trim() || status === "loading"}
          className="absolute bottom-1 right-1"
        />
      </PromptInput>
    </main>
  );
}
```

- [ ] **Step 2: Verify page renders**

Open `http://localhost:3000/chat` in browser.
Expected: page loads with empty state, textarea, and submit button.

- [ ] **Step 3: Verify end-to-end flow**

Type a question and submit.
Expected: user message appears, loading state shows, assistant response appears with the stub text.

- [ ] **Step 4: Commit**

```bash
git add app/chat/page.tsx
git commit -m "feat: add chat page with ai-elements components"
```

---

### Task 4: Add chat link to navbar

**Files:**
- Modify: `src/components/navbar.tsx`

**Interfaces:**
- Consumes: nothing
- Produces: "المحادثة" link in nav, visible on desktop and mobile

- [ ] **Step 1: Add chat link to navLinks array**

Add `{ href: "/chat", label: "المحادثة" }` to the `navLinks` array.

```tsx
const navLinks = [
  { href: "/", label: "الرئيسية" },
  { href: "/quran", label: "القرآن الكريم" },
  { href: "/hadith", label: "الأحاديث النبوية" },
  { href: "/themes", label: "المواضيع" },
  { href: "/chat", label: "المحادثة" },
  { href: "/favorites", label: "المفضلة" },
];
```

- [ ] **Step 2: Verify navbar shows the link**

Open `http://localhost:3000` — the navbar should include "المحادثة" link.

- [ ] **Step 3: Commit**

```bash
git add src/components/navbar.tsx
git commit -m "feat: add chat link to navbar"
```
